{
  "name": "arsenic",
  "version": "1.0.0",
  "description": "an open-source website",
  "engines": {
    "npm": ">=9.0.0",
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "worker:deploy": "wrangler deploy",
    "worker:dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
